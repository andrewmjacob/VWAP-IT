name: Release
on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
      - name: Build & Push Docker (placeholder)
        run: |
          docker build -t tip:latest .
          echo "Push to registry step goes here"
